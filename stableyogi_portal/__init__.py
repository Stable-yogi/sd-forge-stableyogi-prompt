"""
StableYogi integration for Forge.

Sources are described as data in `providers.json`, so adding or changing one is a config edit,
not a code change.
"""

from . import providers, client, buffer  # noqa: F401
